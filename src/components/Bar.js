import styled from "styled-components";
import BoxBase from "./Box";

const Bar = styled(BoxBase)`
  min-width: 250px;
  display: flex;
  margin-bottom: 12px;
  justify-content: space-between;
`;

export default Bar;
